# Copy to .env and fill in. See docs/GEMINI_API_KEYS.md for key setup.

# Optional: shared fallback when no per-user key is set in Settings.
GEMINI_API_KEY=
OPENAI_API_KEY=
FAL_KEY=

# CORS: comma-separated origins. Default * is fine for local dev.
# CORS_ORIGINS=http://localhost:5173

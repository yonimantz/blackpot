rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All reads/writes go through the FastAPI backend (Firebase Admin SDK).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
